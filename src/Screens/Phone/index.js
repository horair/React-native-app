import React from 'react';
import {Text, View} from 'react-native';

import styles from './styles';

const Phone = props => {
  return (
    <View>
      <Text>This is Phone screen.</Text>
    </View>
  );
};

export default Phone;
